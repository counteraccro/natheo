{% extends 'admin/admin_base.html.twig' %}

{% block title %}{{ 'mail.page_title' | trans({}, 'mail') }}{% endblock %}

{% block body %}
    <h1>{{ 'mail.page_title_h1' | trans({}, 'mail') }}</h1>

    {{ breadcrumb(breadcrumb) }}

    <button type="button" class="btn btn-secondary float-end ms-2" data-bs-toggle="modal" data-bs-target="#help-grid-mail">
        <i class="bi bi-question-circle"></i>
    </button>

    <p>{{ 'mail.description' | trans({}, 'mail') }} <br />
        <i class="bi bi-info-circle"></i> {{ 'mail.info' | trans({'icon': '<i class="bi bi-send-check"></i>'}, 'mail') | raw }}
    </p>


    <div {{ vue_component('Admin/GenericGrid', {'url' : path('admin_mail_load_grid_data', {'page' : page, 'limit' : limit}), 'page' : page, 'limit' : limit}) }}></div>

    <twig:Admin:ModaleComponent id="help-grid-mail" bgColor="bg-secondary">
        <twig:block name="header">
            <i class="bi bi-question-circle"></i> {{ 'mail.index.popin.help.title' | trans({}, 'mail') }}
        </twig:block>

        <div class="row">
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm mb-1"><i class="bi bi-send-check-fill"></i></button> {{ 'mail.index.popin.help.send' | trans({}, 'mail') }}
            </div>
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm mb-1"><i class="bi bi-pencil-fill"></i></button> {{ 'mail.index.popin.help.edit' | trans({}, 'mail') }}
            </div>
        </div>
    </twig:Admin:ModaleComponent>

{% endblock %}
