{% extends 'admin/admin_base.html.twig' %}

{% block title %}{{ 'faq.index.page_title' | trans({}, 'faq') }}{% endblock %}

{% block body %}
    <h1>{{ 'faq.index.page_title_h1' | trans({}, 'faq') }}</h1>

    {{ breadcrumb(breadcrumb) }}

    <button type="button" class="btn btn-secondary float-end ms-2" data-bs-toggle="modal" data-bs-target="#help-grid-faq">
        <i class="bi bi-question-circle"></i>
    </button>
    <a href="{{ path('admin_faq_add') }}" class="float-end btn btn-secondary"><i class="bi bi-question-circle-fill"></i> {{ 'faq.index.btn.new' | trans({}, 'faq') }}</a>

    <p>
        {{ 'faq.index.description' | trans({}, 'faq') }} <br/>
        {#  {{ 'tag.index.description2' | trans({'icon' : '<i class="bi-eye-slash-fill"></i>'}, 'tag') | raw }} #}<br/>
    </p>


    <div {{ vue_component('Admin/GenericGrid', {'url' : path('admin_faq_load_grid_data',  {'page' : page, 'limit' : limit}), 'page' : page, 'limit' : limit, 'activeSearchData' : true}) }}></div>

    <twig:Admin:ModaleComponent id="help-grid-faq" bgColor="bg-secondary">
        <twig:block name="header">
            <i class="bi bi-question-circle"></i> {{ 'faq.index.popin.help.title' | trans({}, 'faq') }}
        </twig:block>

        <div class="row">
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm mb-1"><i class="bi bi-eye-slash-fill"></i></button> {{ 'faq.index.popin.help.disabled' | trans({}, 'faq') }}
            </div>
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm mb-1"><i class="bi bi bi-eye-fill"></i></button> {{ 'faq.index.popin.help.enable' | trans({}, 'faq') }}
            </div>
        </div>
        <div class="row">
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm mb-1"><i class="bi bi-trash"></i></button> {{ 'faq.index.popin.help.delete' | trans({}, 'faq') }}
            </div>
            <div class="col">
                <button type="button" class="btn btn-secondary btn-sm mb-1"><i class="bi bi bi-pencil-fill"></i></button> {{ 'faq.index.popin.help.edit' | trans({}, 'faq') }}
            </div>
        </div>
    </twig:Admin:ModaleComponent>

{% endblock %}