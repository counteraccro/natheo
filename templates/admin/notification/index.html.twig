{% extends 'admin/admin_base.html.twig' %}

{% block title %}{{ 'notification.page_title' | trans({}, 'notification') }}{% endblock %}

{% block body %}
    <h1>{{ 'notification.page_title_h1' | trans({}, 'notification') }} </h1>

    {{ breadcrumb(breadcrumb) }}

    <p><i class="bi bi-bell-fill"></i> {{ 'notification.description' | trans({}, 'notification') }}
    </p>


        <div {{ vue_component('Admin/Notification/NotificationList', {'url' : path('admin_notification_list'), 'page' : page, 'limit' : limit}) }}></div>

{% endblock %}

